<UserControl x:Class="Views.HeaderControl"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:local="clr-namespace:SpinTheWheel"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             d:DesignHeight="80"
             d:DesignWidth="800"
             mc:Ignorable="d">
    <UserControl.Effect>
        <DropShadowEffect BlurRadius="10"
                          Opacity="0.5"
                          ShadowDepth="5"
                          Color="Black" />
    </UserControl.Effect>
    <Grid Height="90" Background="{StaticResource PrimaryBrush}">
        <ContentPresenter Margin="10,0"
                          VerticalAlignment="Center"
                          Content="{Binding Content, RelativeSource={RelativeSource AncestorType=UserControl}}" />
    </Grid>
</UserControl>